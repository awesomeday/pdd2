import { IAnswersState } from './types';

const INITIAL_STATE: IAnswersState = {};

export const answersReducer = (state: IAnswersState = INITIAL_STATE, action): IAnswersState => {
    return state;
};
